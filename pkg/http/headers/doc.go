/*
Package headers provides constants for common HTTP header names and values.

Use this package to reference standard HTTP headers in a type-safe manner
without relying on string literals. This helps prevent typos and provides
consistent header usage across the codebase.

The package defines header keys, like Content-Type, along with common header
values, like application/json.
*/
package headers
